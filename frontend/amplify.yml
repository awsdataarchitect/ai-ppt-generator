version: 1
frontend:
  phases:
    build:
      commands:
        - echo "Static HTML/JS deployment - no build needed"
  artifacts:
    baseDirectory: /
    files:
      - '**/*'
  cache:
    paths: []
